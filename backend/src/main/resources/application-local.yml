spring:
  config:
    activate:
      on-profile: local
  datasource:
    password: ${FANSCALE_DB_PASSWORD:fanscale_local_only}

springdoc:
  api-docs:
    enabled: ${FANSCALE_OPENAPI_ENABLED:true}
  swagger-ui:
    enabled: ${FANSCALE_OPENAPI_ENABLED:true}

fanscale:
  environment: local
